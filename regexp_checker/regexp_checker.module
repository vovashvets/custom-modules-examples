<?php

/**
 * @file
 * Contains regexp_checker.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function regexp_checker_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the regexp_checker module.
    case 'help.page.regexp_checker':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Offers an internal page for check Regular Expressi�ons in a Drupal Context.') . '</p>';
      return $output;

    default:
  }
}
